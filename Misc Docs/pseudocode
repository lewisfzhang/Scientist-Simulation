previous effort calc for young scientist in time period t
	if idea == t:
		prev_effort = 0
	if idea == t-1:
		prev_effort = all effort on idea t-1 in time < t

previous effort calc for old scientist in time period t+1
	if idea == t+1:
		prev_effort = 0
	if idea < t+1:
		prev_effort = all effort on idea in time < t+1